export enum ConsoleStatus {
  DRAFT = 'DRAFT',
  RELEASE = 'RELEASE',
}

export enum OSType {
  WIN = 'Win',
  MAC = 'Mac',
  RESTS = 'rests',
}

export enum ConnectionKind {
  Private = 'PRIVATE',
  Shared = 'SHARED'
}
